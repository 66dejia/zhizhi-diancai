{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && cd server && npm install && cd .. && npm run build"
  },
  "deploy": {
    "startCommand": "node server/index.js",
    "healthcheckPath": "/"
  }
}