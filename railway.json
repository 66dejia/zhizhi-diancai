{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && cd server && npm install && cd .. && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && node index.js",
    "healthcheckPath": "/"
  }
}